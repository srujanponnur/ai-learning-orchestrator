# Project 1 — AI Learning Orchestrator
# Copy to .env (gitignored) OR set globally via `setx`. The notion MCP reads NOTION_TOKEN.

# Notion internal-integration secret (starts with ntn_). Connect the integration to your target page.
NOTION_TOKEN=ntn_your_secret_here
